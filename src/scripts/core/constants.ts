export const MOD_PREFIX = "exile";

export const DynamicProperties = {
  playerStats: `${MOD_PREFIX}:player_stats`,
  bossProgress: `${MOD_PREFIX}:boss_progress`,
  playerGear: `${MOD_PREFIX}:player_gear`,
  portalStructures: `${MOD_PREFIX}:portal_structures`
};

export const BossIds = {
  rochatus: "rochatus"
};

export const ItemIds = {
  portalShard: `${MOD_PREFIX}:portal_shard`,
  azurion: `${MOD_PREFIX}:azurion`,
  oricalum: `${MOD_PREFIX}:oricalum`
};

export const EntityIds = {
  rochatus: `${MOD_PREFIX}:rochatus`
};
